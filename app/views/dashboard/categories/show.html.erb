<div class="container">
  <div class="card">
    <div class="card">
      <div class="card-body">
        <strong>Title:</strong>
        <%= @category.title %>
      </div>
    </div>
  </div>

  <%= link_to 'Edit', edit_dashboard_category_path(@category) %> |
  <%= link_to 'Back', dashboard_categories_path %>
</div>
