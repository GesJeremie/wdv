/**
 * We use bootstrap as a UI kit, no time for custom css.
 * We import a custom file "bootstrap" to be able to
 * style some really small stuff.
 */

@import 'vendor/icons';
@import 'bootstrap';
@import 'dashboard/hooks/bootstrap';
